# Health Data Sync

A modularized Streamlit application for syncing health data from Garmin to Google Sheets and exporting Cronometer data via a Go backend.

## Prerequisites

- [Python 3.10+](https://www.python.org/downloads/)
- [Git](https://git-scm.com/)
- (For Cronometer Sync) [Go](https://go.dev/dl/)

## Setup

### 1. Clone the Repository

```bash
git clone <repository-url>
cd health_data_app
```

### 2. Environment Setup

**macOS / Linux:**
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

**Windows (PowerShell):**
```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

*(Note: If you are on Windows and don't have `pip` installed, run `python -m ensurepip` first.)*

## Authentication & Configuration

The app uses local files for Google access and Garmin login state. These files are **private** and should never be committed to Git.

### What You Need
- `credentials.json`: the Google OAuth Desktop client file you download from Google Cloud.
- `google_token.json`: the token file the app creates after you complete Google sign-in.
- `garmin` token folder: local Garmin auth state saved by the app.
- `data/`: local output folder for reports and exported CSV files.

### Important Windows Note
The README may show example paths from a macOS machine, but you do **not** need to use those exact paths on Windows. In the app, open the **Settings** tab and replace every path with a Windows path on your own machine, for example:

- `C:\Users\<you>\Documents\health_data_app\secrets\google\credentials.json`
- `C:\Users\<you>\Documents\health_data_app\secrets\google\google_token.json`
- `C:\Users\<you>\Documents\health_data_app\secrets\garmin`
- `C:\Users\<you>\Documents\health_data_app\data\reports`

Use forward or backslashes in the Streamlit text boxes if you prefer, but keep the path valid on Windows.

### Google Credentials Setup
1. Go to the [Google Cloud Console](https://console.cloud.google.com/).
2. Create or select a project.
3. Enable the Google Sheets API.
4. Create an **OAuth client ID** for a **Desktop app**.
5. Download the JSON file Google gives you.
6. Save it somewhere local on your Windows machine, for example:
   `C:\Users\<you>\Documents\health_data_app\secrets\google\credentials.json`
7. In the app, open the **Settings** tab and set **Google credentials file** to that exact path.

### Google Token Setup
You do **not** create the token file by hand.

1. Make sure the Google credentials file path is correct.
2. Open the app and go to the **Settings** tab.
3. Click **Start Google auth**.
4. A browser window opens with a Google sign-in page.
5. Sign in and approve access.
6. Google gives you an authorization code.
7. Paste that code into the app.
8. Click **Save Google token**.

The app then writes `google_token.json` to the path shown in **Google token file**. You can keep that file in any local folder you want, but it must stay on your machine and should not be committed.

### What To Pay Attention To
- The Google credentials file is the downloaded OAuth client JSON, not a token.
- The token file is generated by the app after authorization.
- Both files must point to real local Windows paths.
- If either path is wrong, Google Sheets sync will fail.
- If you move the files later, update the paths in **Settings**.
- If the token is deleted or becomes invalid, run the Google auth flow again.
- Keep the `credentials.json` and `google_token.json` files private.

## Run the Application

**macOS / Linux:**
```bash
source .venv/bin/activate
streamlit run app.py
```

**Windows:**
```powershell
.\.venv\Scripts\Activate.ps1
streamlit run app.py
```

The app will launch at `http://localhost:8501`.

## Security Note

All sensitive data, environment files, and local workspaces are strictly excluded from version control via `.gitignore`. 
- **NEVER** commit `credentials.json`, the `google_token.json` file, the `tokens/` folder, or any `.env` files.
- If you accidentally commit a secret, rotate your API keys immediately.
